<Window
    x:Class="HLSDownloader.Windows.PlaybackProbeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="再生解析（α）">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Padding="12" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button x:Name="BackButton" Click="BackButton_OnClick" Content="戻る" IsEnabled="False" />
            <Button Grid.Column="1" Click="ReloadButton_OnClick" Content="再読込" />
            <TextBox
                x:Name="AddressTextBox"
                Grid.Column="2"
                VerticalContentAlignment="Center"
                KeyDown="AddressTextBox_OnKeyDown" />
            <Button Grid.Column="3" Click="GoButton_OnClick" Content="移動" />
            <Button Grid.Column="4" Click="DoneButton_OnClick" Content="候補一覧へ戻る" />
        </Grid>

        <InfoBar
            x:Name="ProbeInfoBar"
            Grid.Row="1"
            Margin="12,0,12,8"
            IsClosable="False"
            IsOpen="True"
            Message="ページ内の再生ボタンを操作してください。候補 0 件"
            Severity="Informational" />

        <Grid Grid.Row="2">
            <WebView2
                x:Name="Browser"
                CoreWebView2Initialized="Browser_OnCoreWebView2Initialized"
                NavigationCompleted="Browser_OnNavigationCompleted" />
            <ProgressRing
                x:Name="BrowserProgress"
                Width="42"
                Height="42"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsActive="True" />
        </Grid>
    </Grid>
</Window>
